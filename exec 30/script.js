for (var i = 1; i <= 59; i++) {    // loop para cada parte
    console.log(`#### Loop ${i} ####`);  // imprime o número da parte
    for (var j = 1; j <= 5; j++) {   // loop para cada número dentro da parte
      console.log(`Passo ${j}.`);     // imprime o número atual
    }
  }