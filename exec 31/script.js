function contar() {
    var vezes = Number.parseInt(document.getElementById(inicio).value)
    var vezes2 = Number.parseInt(document.getElementById(fim).value)

    while (vezes < 10) {
        resultado.innerHTML = `Passo ${vezes}.`
        }
        vezes2++
    }